// let start = false; // used to start game when start button is clicked

// const Start = (bool, name) => {
//     // call this when start is clicked
//     // set start equal to true and only allow player and ai to click once start is true
//     start = bool;
//     console.log(name);
//     const startBtn = document.querySelector(`.${String(name)}`);
//     if (start && startBtn) startBtn.remove(); 
//     return (
//         <>
//             <button className="start-btn" onClick={() => {
//                 Start(true, "start-btn");
//             }}>Start Game!</button>
//         </>
//     )
// }

// export {
//     Start
// }